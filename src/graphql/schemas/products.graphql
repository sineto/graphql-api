type Product {
  id: ID!
  name: String
  price: Float
}

type ProductOutput {
  data: [Product!]!
  hasNext: Boolean
}

input ProductFindAllOptions {
  limit: Int
  offset: Int
  orderBy: String
  order: String
  categoryId: Int
}

input ProductInput {
  name: String!
  price: Float!
}
